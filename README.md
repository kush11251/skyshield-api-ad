# SkyShield API
A high-performance API gateway for securing cloud-based applications

## Installation
1. Clone the repository
2. Install dependencies using `npm install`
3. Run the application using `npm start`

## Usage
The API gateway provides the following endpoints:
* `/users`: Retrieve a list of users
* `/metrics`: Retrieve application metrics